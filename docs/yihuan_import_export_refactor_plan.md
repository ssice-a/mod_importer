# 旧导入导出计划已废弃

这份文档原本面向旧 Yihuan BoneStore 导出器，包含 `ResourceYihuanRuntime*`、`BoneStore INI`、`CharacterMetaTable`、`R16` 顶点窗口等旧规则。

当前项目已经改用 NTMI fast path：

- 新运行时规范：`docs/ntmi_export_runtime_spec.md`
- 新插件重构计划：`docs/ntmi_plugin_refactor_plan.md`

后续实现只按新规范推进，不再兼容旧导出格式。
